# AI 预测 vs 实际开奖对比：历史期号选择

## 问题
原来"AI 预测 vs 实际开奖对比"只能对比**最新一期**开奖。若用户保存的是历史期预测（例如第 26090 期），回头想看该期实际开奖对比时，页面提示"暂无该期的预测记录"。

## 改动
1. **后端：`ai_predict.py`**
   - `analyze_saved_predictions(lottery_type, target_code=None)` 新增 `target_code` 参数。
   - 未传 `target_code`：保持原逻辑，自动对比最新开奖期。
   - 传入 `target_code`：在已保存记录中定位该期，并在开奖 CSV 中按同一期号查找实际开奖。
   - 若该期尚未开奖，返回明确提示"第 X 期尚未开奖（最新开奖期号：Y），请等待开奖后再对比"。

2. **前端：`app.py`**
   - 在"对比已保存的预测记录与实际开奖"按钮上方增加 `st.selectbox`。
   - 下拉选项来自**已保存的预测记录期号**。
   - 选中后点击对比按钮，即可看到该期的开奖号码与最佳命中组。
   - 对比结果标题从"最新开奖"改为"开奖号码"，避免历史期歧义。

## 验证
- `py_compile app.py ai_predict.py` 通过。
- 临时功能测试：
  - 指定 26090 期：正确匹配 26090 期开奖数据，命中 3 位。
  - 指定 26091 期：正确匹配 26091 期开奖数据，命中 2 位。
  - 指定未保存的 26095 期：正确提示"未找到预测记录"。
  - 不指定 code：保持默认最新期逻辑。
- AppTest：6 彩种 × 福彩/体彩 共 12 组合 AI 页渲染 0 失败。

## 注意
- 改动在 `app.py` 和 `ai_predict.py`。如果正在运行旧的 Streamlit server，需要 Ctrl+C 重启一次才能生效。
