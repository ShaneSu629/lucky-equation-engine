# 体彩版「组合配比策略」开发完成

## 回答你的两个问题
1. **能不能做？** 可以，且与福彩版 1:1 对称。
2. **网上案例？** 有，主流是「复式 + 胆拖 + 预算纪律」框架（头条《复式胆拖完整攻略》《大乐透怎么买才合算》、17500.cn《四大攻略》、weicainet《复式投注优选技巧》），均强调开奖完全随机、理性购彩。

## 实现方案（你确认的设计）
| 角色 | 福彩版 | 体彩版（新增） |
|------|--------|---------------|
| 核心主投 | 双色球 | **大乐透** |
| 对冲 A | 快乐8选一/选四 | **排列三 组选六**（每日高频，173 元） |
| 对冲 B | 福彩3D 组选六 | **七星彩 七位直选**（低频大回血） |

功能对称包含：预算/回血率分析、号码推荐、一键复制、AI 智能优化、保存预测记录。

## 改动文件
- `generate_picks.py`：新增 `gen_pl3_group6` / `gen_qxc_pick7` 及对应 formatter。
- `ai_predict.py`：新增 `ai_optimize_hedge_sports(dlt_bets, hedge_strategy)` + 3 个历史偏置生成器，复用已有 `_calculate_*` 特征工程。
- `app.py`：hedge 页体彩分支从「请切换福彩」改为 `_render_sports_hedge()`；福利分支抽成 `_render_welfare_hedge()`。

## 顺手修掉的结构性 Bug
原福利对冲正文在 `elif` 体级别会**无条件执行**，导致选体彩时「对冲单注数」slider 与福利 slider 同 ID 冲突（StreamlitDuplicateElementId 崩溃）。已用两个独立函数 `if/else` 隔离，彻底解决。

## 验证
- `py_compile` 全过。
- AppTest 渲染全 5 页 × 福彩/体彩（10 组合）**0 失败**。
- 体彩对冲功能测试（生成器 + AI mock，方案 A/B + 容错解析）全过。

## 提醒
- 改动在 `app.py` 入口，**需重启 Streamlit server** 生效。
- 这些"策略"本质是历史统计规律的娱乐参考，开奖独立随机，已在对冲页保留理性购彩声明。
