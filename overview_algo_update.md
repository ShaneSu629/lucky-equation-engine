# 彩票算法全网调研 + 引擎增强总结

## 一、全网调研结论（2025–2026）

搜索了 PowerPredict、LottoProphet、luckpot、17500 数学模型文、CSDN 智能选号模型等主流项目，共识：

- **彩票本质是 i.i.d. 随机事件**，任何算法都无法真正预测中奖，只能提升「统计合理性 / 组合优选」，与项目已有免责声明一致。
- 主流**轻量且有效**的方法：`频率/遗漏/马尔可夫/蒙特卡洛/贝叶斯融合` + `区间轮动热度` + `和值/连号/同尾数偏好` + `生成多样性（汉明距离 / 回避机制）`。
- **深度学习（LSTM/Transformer/LSTM-CRF）不建议引入**：需要 tensorflow/pytorch 重依赖、本地无 GPU、对 i.i.d. 事件效果有限，违背轻量定位。

项目原本已有频率/衰减/遗漏/马尔可夫/贝叶斯/蒙特卡洛/多维约束，但缺上述三个被反复验证的轻量增强——本次补齐。

## 二、本次新增/修改（全部在 `enhanced_predict.py`，纯 numpy，零新依赖）

1. **区间轮动热度** — 新增 `calculate_zone_heat` + `_zone_ranges_for`，并将热度作为新信号融入 `bayesian_fusion_scores`（新增可选 `zone_heat`/`w_zone` 参数，权重自动归一化，向后兼容）。偏好当前活跃区间号码。
2. **和值 / 连号 / 同尾数偏好** — 新增 `score_combination_preferences`，在 `predict_ssq/dlt/kl8` 生成时作为温和过滤（阈值 0.35/0.35/0.30，只丢弃极端不合理组合）。
3. **生成多样性** — 新增 `_hamming_vs_used`，组间强制最小汉明距离（双色球/大乐透 ≥2、快乐8 ≥3），避免一次生成的 N 组选号扎堆雷同。
4. **蒙特卡洛历史回溯验证** — 新增 `monte_carlo_backtest`：用融合/蒙特卡洛/马尔可夫 Top-K 对最近 30 期真实开奖做命中统计；`get_ensemble_prediction` 的 `model_contributions` 改为基于回溯的**真实吻合度**动态估计（替代原写死的 0.40/0.30/0.30），并新增 `backtest` 返回字段。

## 三、顺带修复的 Bug

- **位置制彩票（七星彩/排列三/福彩3D）集成预测只生成 4 组**：号码池仅 0–9，原「高频 Top-K 滑动窗口」最多只能凑出 4 种组合且无兜底。改为直接委托分位独立的 `predict_qxc/predict_pl3/predict_fcsd` 生成，确保足量且不重复。

## 四、验证结果

- 编译：5 模块全过。
- 功能测试：6 彩种集成预测全过；偏好评分配比正确（均衡组合 0.71 > 极端连号 0.15）；ssq 5 组最小汉明距离 = 2；回溯输出正常。
- AppTest：全 5 页 × 福彩/体彩 = 10 组合渲染 0 失败。

## 五、一个重要事实提醒

回溯结果显示：双色球融合命中率 ≈1.03，随机期望 ≈1.09——**算法历史命中率与随机基本持平**，印证「彩票无法被算法战胜」。新增的所有增强是让推荐组合更符合统计特征、更具多样性，而非提高中奖概率。这更说明项目顶部的理性购彩声明是必要的，请勿将「模型贡献」误读为「中奖保证」。
