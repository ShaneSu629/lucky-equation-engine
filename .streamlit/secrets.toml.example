# Streamlit Secrets 配置模板
# 复制本文件为 secrets.toml 并填入真实值
# 云端部署时在 Streamlit Cloud → Settings → Secrets 中配置相同内容
#
# 注意：secrets.toml 已被 .gitignore 排除，不会被提交到 GitHub

[ai_config]
api_key = "sk-xxxxxxxxxxxxxxxx"
base_url = "https://api.siliconflow.cn/v1"
model = "deepseek-ai/DeepSeek-R1"
